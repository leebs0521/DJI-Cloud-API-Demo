package com.dji.sdk.cloudapi.flightarea;

/**
 * 비행 구역 조회 요청 클래스
 * 
 * 이 클래스는 DJI Cloud API에서 비행 구역 조회 요청을 정의하는 클래스입니다.
 * 현재는 빈 클래스로 구현되어 있으며, 향후 비행 구역 조회 시 필요한 
 * 파라미터들(예: 워크스페이스 ID, 필터 조건 등)을 추가할 수 있도록 
 * 확장 가능한 구조로 설계되어 있습니다.
 * 
 * @author sean
 * @version 1.7
 * @date 2023/10/17
 */
public class FlightAreasGetRequest {
    // 현재는 빈 클래스로 구현되어 있음
    // 향후 비행 구역 조회 시 필요한 파라미터들을 추가할 수 있음
    // 예: workspaceId, filter, pagination 등의 필드
}
